{
  "name": " ",
  "version": "0.0.1",
  "scripts": {
    "clear": "rimraf dist",
    "build": "npm run clear && parcel build src/*.html --no-minify",
    "dev": "parcel -p 3000 src/index.html --open --no-autoinstall",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "prettier": "^2.8.4",
    "rimraf": "^4.4.0"
  },
  "dependencies": {
    "@svgdotjs/svg.js": "^3.1.2",
    "gsap": "^3.11.5",
    "plyr": "^3.7.7"
  }
}
